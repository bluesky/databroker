.. _whatsnew:

What's new
**********

v0.5.0
------

 - extended public API to get the ``Resource`` from a datum eid via `resource_given_eid`.
 - taught the `FileStoreRO` object to override the ``root`` part of a ``Resource`` via
   the `root_map` attribute.

__author__ = 'edill'

from enaml.widgets.api import Container, Label

enamldef PlotView(Container):
    attr scalar_model
    Label:
        text << scalar_model.name

enamldef PlotControls(Container):
    attr scalar_model
    Label:
        text << scalar_model.name
